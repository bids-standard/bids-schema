"""bids_schema — tooling for the bids-schema archive.

Sub-packages:

- ``bids_schema.collect``  — collect PR stats and BEP registration timestamps.
- ``bids_schema.render``   — render ``PRs/README.md`` and ``BEPs/README.md``.
- ``bids_schema.metadata`` — atomic read-modify-write helpers, schema constants.
"""

__version__ = "0.1.0"
